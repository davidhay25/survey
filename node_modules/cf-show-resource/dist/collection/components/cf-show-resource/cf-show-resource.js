import { Component, Prop, h } from '@stencil/core';
export class MyComponent {
    componentWillLoad() {
        //let json = JSON.parse(this.resource)
    }
    render() {
        return [
            h("div", null, "Show the details of a resource"),
            h("pre", null, this.resource)
        ];
    }
    static get is() { return "cf-show-resource"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["cf-show-resource.css"]
    }; }
    static get styleUrls() { return {
        "$": ["cf-show-resource.css"]
    }; }
    static get properties() { return {
        "resource": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "The FHIR resource as a json string"
            },
            "attribute": "resource",
            "reflect": false
        }
    }; }
}
